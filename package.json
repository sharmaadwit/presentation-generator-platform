{
  "name": "presentation-generator-platform",
  "version": "1.0.0",
  "description": "AI-powered platform for automated presentation generation from existing slides",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:ai\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:ai": "cd ai-service && python app.py",
    "build": "npm run install:backend && npm run build:backend",
    "install:backend": "cd backend && npm install",
    "build:backend": "cd backend && npm run build",
    "start": "cd backend && npm start",
    "start:production": "cd backend && npm start",
    "install:all": "cd backend && npm install && cd ../ai-service && pip install -r requirements.txt"
  },
  "keywords": ["presentation", "ai", "automation", "slides", "generation"],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
